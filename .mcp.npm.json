{
    "mcpServers": {
        "redmine": {
            "command": "npx",
            "args": ["-y", "@thelabnyc/redmine-mcp@0.5.0"],
            "env": {
                "REDMINE_URL": "${REDMINE_URL}",
                "REDMINE_API_KEY": "${REDMINE_API_KEY}"
            }
        }
    }
}
